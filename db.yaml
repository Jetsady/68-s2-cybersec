  services:
    s2db:
      image: postgres:lastest
      container_name: 68-s2-database
      port:
       - ${DATABASE_PORT}:5432
      environment:
       POSTGRES_USER: ${DATABASE_USER}
       POSTGRES_PASSWORD: ${DATABASE_PASS}
       POSTGRES_DB: ${DATABASE_NAME}
